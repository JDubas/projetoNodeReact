
export default function ModelInserir(props){
    document.title = "Cadastrar"
    if(props.open){
        return(
            <div>
                <h1>Modal Adicionar</h1>
            </div>
        );
    }
}